mutation banIp(
    $ip: String!
    $jail: String
    $durationSeconds: Int
    $reason: String
) {
    bruteForceLoginProtection {
        banIp(
            ip: $ip
            jail: $jail
            durationSeconds: $durationSeconds
            reason: $reason
        )
    }
}
