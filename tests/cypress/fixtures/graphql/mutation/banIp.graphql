mutation banIp(
    $ip: String!
    $jail: String
    $durationSeconds: Int
    $reason: String
) {
    bruteForceLoginProtectionBanIp(
        ip: $ip
        jail: $jail
        durationSeconds: $durationSeconds
        reason: $reason
    )
}
