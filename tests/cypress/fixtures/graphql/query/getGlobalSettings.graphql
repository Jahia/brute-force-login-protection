query getGlobalSettings {
    bruteForceLoginProtectionGlobalSettings {
        activated
        whitelistIps
        ignorePatterns
        trustProxyHeader
        emailEnabled
        emailRecipient
        webhookUrl
        webhookSecretConfigured
        auditLogMaxEntries
        recidiveFactor
        maxBanTimeSeconds
    }
}
