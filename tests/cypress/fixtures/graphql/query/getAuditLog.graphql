query getAuditLog($limit: Int) {
    bruteForceLoginProtection {
        auditLog(limit: $limit) {
            id
            timestamp
            event
            ip
            jail
            source
            details
        }
    }
}
